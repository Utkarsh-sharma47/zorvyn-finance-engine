# Local dev: use `npm run dev` — requests go to Vite (:5173); vite.config.js proxies `/api` → FastAPI (:8000).
# Do not set VITE_API_BASE_URL for local dev unless you know you need a full URL (that bypasses the proxy).
