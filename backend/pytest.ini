[pytest]
# Discover tests only under backend/tests/ (never ad-hoc test files at repo root).
testpaths = tests
pythonpath = .
# Test database URL is also set at the top of tests/conftest.py before any app imports,
# so the SQLModel engine never points at a real Postgres instance during collection.
